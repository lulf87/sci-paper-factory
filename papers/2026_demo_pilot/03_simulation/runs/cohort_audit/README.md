# Cohort Audit Outputs

This directory stores cohort-audit artifacts derived from the first-pass planning instance table.

Current status:
- `eligible_main_cohort_v1.csv` is provisional, not final.
- `cohort_audit_summary.json` reflects the current audit pass.
- `exclusion_log.csv`, `subgroup_labels.csv`, and `roi_metadata.csv` are preprocessing artifacts.
- Final main cohort freeze still requires formal operational rules for:
  - cystic_interference
  - multifocal independence
