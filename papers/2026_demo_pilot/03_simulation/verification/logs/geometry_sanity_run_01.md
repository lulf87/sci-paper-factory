# Geometry sanity run 01

Date:
- FILL_ME

Command:
```bash
python papers/2026_demo_pilot/03_simulation/src/run_geometry_sanity_checks.py
```

Result:
- PASS

Notes:
- `run_geometry_sanity_checks.py` currently runs built-in deterministic unit checks only.
- It does not take runtime CSV arguments in the current repo version.
- If the script prints `All geometry sanity checks passed.`, record PASS and keep the raw terminal output in `geometry_sanity_run_01.txt`.
